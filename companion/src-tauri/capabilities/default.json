{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Privileged main / settings window only",
  "windows": ["main"],
  "permissions": ["core:default", "allow-main-commands"]
}
